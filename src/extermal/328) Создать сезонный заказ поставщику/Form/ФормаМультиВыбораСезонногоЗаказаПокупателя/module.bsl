
Процедура ДействияФормыВыбрать(Кнопка)
	// Вставить содержимое обработчика.
	Заказ = Документы.ЗаказПокупателяСезонный.ПолучитьФормуВыбора().ОткрытьМодально();
	Если ЗначениеЗаполнено(Заказ) Тогда 
		СтрХ = ТабличноеПолеЗаказов.Добавить();
		СтрХ.Ссылка = ЗАказ;
		СтрХ.Контрагент = ЗАказ.Контрагент;
	КонецЕсли;	
	
КонецПроцедуры

Процедура ТабличноеПолеЗаказовВыборЗначения(Элемент, СтандартнаяОбработка, Значение)
	// Вставить содержимое обработчика.
КонецПроцедуры

Функция ПолучитьМассивТоваров()
	Возврат ТабличноеПолеЗаказов.Скопировать();
КонецФункции

Процедура ОКНажатие(Элемент)
	// Вставить содержимое обработчика.
	МассивТоваров = ПолучитьМассивТоваров();	
	Закрыть(МассивТоваров);	
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	Попытка 
		Для каждого СтрХ из СписокЗАказов Цикл
			СтрУ = 	ТабличноеПолеЗаказов.Добавить();
			СтрУ.Ссылка = СтрХ.Ссылка;
			СтрУ.Контрагент = СтрХ.Ссылка.Контрагент;
		КонецЦикла;
	Исключение
		;
	КонецПопытки;		
КонецПроцедуры

Процедура ПриЗакрытии()
	// Вставить содержимое обработчика.
	
КонецПроцедуры

Процедура ПередЗакрытием(Отказ, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	МассивТоваров = ПолучитьМассивТоваров();	
	Закрыть(МассивТоваров);		
КонецПроцедуры
