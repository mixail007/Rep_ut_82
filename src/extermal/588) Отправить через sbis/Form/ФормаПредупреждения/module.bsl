&НаКлиенте
Функция Показать(Текст, ТекстСсылка, АдресСсылка) Экспорт
	// Функция открывает окно сопоставления документов и заполняет все реквизиты на форме	
	ПредупреждениеАдресСсылка = АдресСсылка;
	ПредупреждениеСсылка = ТекстСсылка;
	ПредупреждениеТекст = Текст;
	ЭтаФорма.Открыть();
КонецФункции
&НаКлиенте
Процедура ПредупреждениеСсылкаНажатие(Элемент, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	ЗапуститьПриложение(ПредупреждениеАдресСсылка);
КонецПроцедуры

