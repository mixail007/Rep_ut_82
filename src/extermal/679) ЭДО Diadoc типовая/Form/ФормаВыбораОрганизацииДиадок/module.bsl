Функция ВыбратьОрганизациюДиадок(IDОрганизацииДиадок) Экспорт
	СписокОрганизаций.Очистить();
	тз = ПолучитьМодульПрог("Модуль_Данные1С_СвязиОбъектов").ПолучитьТаблицуЯщиковДиадокОрганизации();
	Для каждого стрИсх Из тз Цикл
		стр = СписокОрганизаций.Добавить();
		стр.Наименование = стрИсх.Представление;
		стр.ID = стрИсх.Значение;
		//стр.ПредставлениеСертификата = стрИсх.ПредставлениеСертификата;
		стр.ОрганизацияСсылка = стрИсх.Объект;
	КонецЦикла;
	СписокОрганизаций.Сортировать("Наименование,ПредставлениеСертификата");
	
	стр = СписокОрганизаций.Найти(IDОрганизацииДиадок, "ID");
	Если стр <> Неопределено Тогда
		ЭлементыФормы.СписокОрганизаций.ТекущаяСтрока = стр;
	КонецЕсли;
	
	Возврат ОткрытьМодально();
КонецФункции

Процедура КнопкаВыполнитьНажатие(Кнопка)
	ЗавершитьВыбор();
КонецПроцедуры

Процедура СписокОрганизацийПередУдалением(Элемент, Отказ)
	Отказ = Истина;
КонецПроцедуры

Процедура СписокОрганизацийПередНачаломИзменения(Элемент, Отказ)
	ЗавершитьВыбор();
КонецПроцедуры

Процедура ЗавершитьВыбор()
	текДанные = ЭлементыФормы.СписокОрганизаций.ТекущиеДанные;
	Если текДанные = Неопределено Тогда
		Предупреждение("Выберите организацию.",, НаименованиеСистемы);
		Возврат;
	КонецЕсли;
	
	Закрыть(Новый Структура("ID, Наименование, ОрганизацияСсылка", текДанные.ID, текДанные.Наименование, текДанные.ОрганизацияСсылка));
КонецПроцедуры

Процедура ПриОткрытии()
		КартинкаЗаголовка= ЭДО_БиблиотекаКартинок().КартинкаЗаголовка;
КонецПроцедуры
