Перем Подробности;

Процедура ПоказатьОшибку(_Заголовок = "", _ОписаниеОшибки, _Подробности= "") Экспорт
	
	Если НЕ ПустаяСтрока(_Заголовок) Тогда
		Заголовок= _Заголовок;
	КонецЕсли;
	
	Подробности= 	_Подробности;
	ОписаниеОшибки= _ОписаниеОшибки;
	
	ОткрытьМодально();
	
КонецПроцедуры

Процедура ПриОткрытии()
	
	УправлениеФормой();
	
КонецПроцедуры

Процедура ОКНажатие(Элемент)
	
	Закрыть();
	
КонецПроцедуры

Процедура ГиперссылкаПодробностиНажатие(Элемент)
	
	ОткрытьЗначение(Подробности);
	
КонецПроцедуры

Процедура УправлениеФормой()
	
	КартинкаЗаголовка= ЭДО_БиблиотекаКартинок().КартинкаЗаголовка;
	
	ЭлементыФормы.ГиперссылкаПодробности.Видимость= ЗначениеЗаполнено(Подробности);
	
КонецПроцедуры
