
Процедура ПриОткрытии()
	
	КартинкаЗаголовка = ЭДО_БиблиотекаКартинок().КартинкаЗаголовка;
	
	Если ЗначениеЗаполнено(URL) Тогда
		ЭлементыФормы.HTMLИстория.Перейти(URL);
	Иначе
		ЭлементыФормы.HTMLИстория.УстановитьТекст(История);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(URL) Тогда
		КлючСохраненияПоложенияОкна = Новый УникальныйИдентификатор();
		Высота = 800;
		Ширина = 1000;
	КонецЕсли;
	
КонецПроцедуры
