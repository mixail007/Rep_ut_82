Функция Печать() Экспорт
	
	ТабДокумент = ПечатьПоЗаказуПокупателя();
	ТабДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт; 
	ТабДокумент.АвтоМасштаб = Истина;
	
	Возврат ТабДокумент;
			
КонецФункции // Печать

Функция ПечатьПоЗаказуПокупателя()
	ТабДокумент = Новый ТабличныйДокумент;
	Макет = ПолучитьМакет("Макет");
	//Запрос = Новый Запрос;
	//Запрос.Текст = 
	//	"ВЫБРАТЬ
	//	|	ЗаказПокупателяСезонныйТовары.Номенклатура.Код КАК Код,
	//	|	ЗаказПокупателяСезонныйТовары.Номенклатура КАК Номенклатура,
	//	|	ЗаказПокупателяСезонныйТовары.Количество КАК Количество
	//	|ИЗ
	//	|	Документ.ЗаказПокупателяСезонный.Товары КАК ЗаказПокупателяСезонныйТовары
	//	|ГДЕ
	//	|	ЗаказПокупателяСезонныйТовары.Ссылка = &Ссылка
	//	|	И ЗаказПокупателяСезонныйТовары.Номенклатура.ВидТовара = &ВидТовара
	//	|	И ЗаказПокупателяСезонныйТовары.Номенклатура.НоменклатурнаяГруппа = &НоменклатурнаяГруппа
	//	|
	//	|УПОРЯДОЧИТЬ ПО
	//	|	ЗаказПокупателяСезонныйТовары.НомерСтроки";
	//	
	//	Запрос.УстановитьПараметр("ВидТовара", Перечисления.ВидыТоваров.Диски);
	//	Запрос.УстановитьПараметр("НоменклатурнаяГруппа", справочники.НоменклатурныеГруппы.НайтиПоКоду("00022"));

	//Запрос.УстановитьПараметр("Ссылка", СсылкаНаОбъект);                                  
	//Результат = Запрос.Выполнить();                                                                        

	//ВыборкаДетальныеЗаписи = Результат.Выгрузить();
    Ном = Неопределено;
	Нагрузка = Неопределено;
	КоличествоИтого = 0;
	Шапка = Макет.ПолучитьОбласть("Шапка");
	Шапка.Параметры.номер = СсылкаНаОбъект.Номер +" от "+ Формат(СсылкаНаОбъект.Дата,"ДЛФ=ДД");
	Областьстроки = Макет.ПолучитьОбласть("Строка");
	ОбластьИтог = Макет.ПолучитьОбласть("Подвал");

	//ОбластьКартинка = Макет.ПолучитьОбласть("строка");
		
	Для каждого стр из СсылкаНаОбъект.Товары Цикл
			ТабДокумент.Вывести(Шапка);

			//ЗаполнитьЗначенияСвойств(Областьстроки, ВыборкаДетальныеЗаписи); 
			Областьстроки.Параметры.Код = стр.Номенклатура.Код;
			Областьстроки.Параметры.Номенклатура = стр.Номенклатура;
			Областьстроки.Параметры.количество = стр.количество;
			ТабДокумент.Вывести(Областьстроки);
		    ТабДокумент.Вывести(ОбластьИтог);
            табДокумент.ВывестиГоризонтальныйРазделительСтраниц();
	КонецЦикла;
	
Возврат ТабДокумент;	
	
КонецФункции