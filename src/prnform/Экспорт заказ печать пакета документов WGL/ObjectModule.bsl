Функция Печать() Экспорт
	
	//Если  НЕ (РольДоступна("ПолныеПрава")или РольДоступна("яштФинДиректор") или РольДоступна("Кассир")) Тогда
	//		Предупреждение("Печать разрешена только для бухгалтерии!",30);
	//	возврат Неопределено;
	//КонецЕсли;	
	
	//Если СсылкаНаОбъект.ДоговорКонтрагента.ТипДоговора <> Справочники.ТипыДоговоров.ШинтрейдЯрославль и 
	//	 СсылкаНаОбъект.ДоговорКонтрагента.Организация <> Справочники.Организации.НайтиПоКоду("00004") Тогда
	//		сообщить("Данная обработка предназначена только для печати отгрузок по договору с типом Шинтрейд Ярославль и по организации Tire Technology!");
	//		возврат Неопределено;
	//Иначе   
		ФормаВыбораМакета = ПолучитьФорму("Форма");
		ФормаВыбораМакета.Открыть();
		возврат Неопределено;
	//КонецЕсли;
	
КонецФункции	