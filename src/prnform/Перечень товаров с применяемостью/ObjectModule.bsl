Функция Печать() Экспорт 
	
	ТабДок = Новый ТабличныйДокумент;
	Макет = ПолучитьМакет("Применяемость");
	Область = Макет.ПолучитьОбласть("Шапка");
	Область.Параметры.Заполнить(СсылкаНаОбъект);
	ТабДок.Вывести(Область);
	Область = Макет.ПолучитьОбласть("Строка");
	Для каждого стр из СсылкаНаОбъект.Товары Цикл
		
		ПолучитьПрименяемостьДискаПоAPITerminal(стр.Номенклатура);
		
		Область.Параметры.Заполнить(стр);
		Область.Параметры.Код = стр.Номенклатура.Код;	
		Отбор = Новый Структура;
		Отбор.Вставить("Номенклатура", стр.Номенклатура);
		Отбор.Вставить("Типоразмер", Справочники.Типоразмеры.ПустаяСсылка());
		Запись = РегистрыСведений.СоответствиеТипоразмеровАвтомобилям.Получить(Отбор);
		Область.Параметры.Применяемость = Запись.Автомобили;
		ТабДок.Вывести(Область);
	конецЦикла;
	табДок.ОтображатьСетку = Ложь;
	ТабДок.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	
	Возврат ТабДок;
	
КонецФункции

                     