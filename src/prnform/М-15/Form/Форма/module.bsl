
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Если СсылкаНаОбъект.Комментарий = "" Тогда
		Сообщить("Контрагент-получатель должен быть указан в Комментарии документа!", СтатусСообщения.Внимание);
	КонецЕсли;
	Печать();
КонецПроцедуры
