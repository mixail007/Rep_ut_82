
Процедура КнопкаВыполнитьНажатие(Кнопка)
	// Вставить содержимое обработчика.
	Если (Пароль = "ujcghbtvrf") тогда
		Закрыть(1);
	Иначе
		Предупреждение("Фигвам !!!");
		Закрыть(0);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОКНажатие(Элемент)
	// Вставить содержимое обработчика.
	Если (Пароль = "склад") тогда
		Закрыть(1);
	Иначе
		Предупреждение("Неверный пароль !!!");
		Закрыть(0);
	КонецЕсли;

КонецПроцедуры
