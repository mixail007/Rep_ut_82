
Процедура КнопкаВыполнитьНажатие(Кнопка)
	//проверим, одного ли поставщика документы
	  отказ = ложь;
	таб = СписокДокументов.Скопировать();
	Таб.Свернуть("Поставщик");
	Если Таб.Количество()>1 тогда
		Сообщить("Выбраны документы разных поставщиков!");
		отказ = Истина;
	конецЕсли;	
	
	Если не отказ тогда
	
	списокДокументов.Сортировать("Проверка Убыв");
	МАС = СписокДокументов.ВыгрузитьКолонку("Проверка");	

	
	ТабДокумент = Печать(Мас);
	ТабДокумент.Показать();
    конецЕсли;
КонецПроцедуры

Процедура СписокДокументовПроверкаПриИзменении(Элемент)
	текСтрока = Элементыформы.СписокДокументов.ТекущиеДанные;
	текСтрока.поставщик = текСтрока.Проверка.поставщик;
КонецПроцедуры
