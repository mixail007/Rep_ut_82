


Процедура КнопкаВыполнитьНажатие(Кнопка)
	
Узел = ПланыОбмена.ОбменУТ_БП.НайтиПоКоду("БП");
Если Не Узел.Пустая() Тогда
   Выборка = ПланыОбмена.ВыбратьИзменения(Узел,Узел.НомерОтправленного + 1);
   
   Пока Выборка.Следующий() Цикл
	   строкаТЗ=ТЗИзменения.Добавить();
	   строкаТЗ.Метаданные=Выборка.Получить().Метаданные().Представление();
	   строкаТЗ.Объект=Выборка.Получить();
   КонецЦикла;	   
   
   ТЗИзменения.Сортировать ("Метаданные");
 //  ТЗИзменения.Показать();
КонецЕсли;

//ПланыОбмена.УдалитьРегистрациюИзменений(Узел,Узел.НомерОтправленного + 1);	
	
КонецПроцедуры
