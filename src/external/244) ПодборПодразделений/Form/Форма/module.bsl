
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Для каждого стр из Таблица Цикл
	Объект = Стр.подразделение.получитьОбъект();
	Объект.Планирование = Истина;
	объект.Записать();	
	конецЦикла;	
КонецПроцедуры
