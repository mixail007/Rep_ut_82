
Процедура ОсновныеДействияФормыЗакрыть(Кнопка)
	Закрыть();
КонецПроцедуры

Процедура КоманднаяПанель1Сформировать(Кнопка)
	// Вставить содержимое обработчика.
	// Отбор по номенклатуре
	Отчет = Отчеты.СтоимостнаяОценкаСкладаВЦенахНоменклатуры.Создать();

	ОтборНоменклатура = Отчет.ПостроительОтчета.Отбор;
	ОтборНоменклатура.Добавить("Номенклатура");
	ОтборНоменклатура.ВидСравнения = ВидСравнения.ВСписке;
	ОтборНоменклатура.Значение = ВыборНоменклатуры.ВыгрузитьКолонку("Номенклатура");
	ОтборНоменклатура.Использование = Истина;
	
	ОтборТипЦен = Отчет.ПостроительОтчета.Отбор.Добавить("ТипЦены");
	ОтборНоменклатура.ВидСравнения = ВидСравнения.Равно;
	ОтборНоменклатура.Значение = ТипЦены;
	ОтборНоменклатура.Использование = Истина;
	
	Отчет.СформироватьОтчет();
	
КонецПроцедуры
