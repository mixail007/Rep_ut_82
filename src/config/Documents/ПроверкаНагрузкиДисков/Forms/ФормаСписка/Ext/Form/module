
Процедура ДействияФормыНепроверенныеКонтейнера(Кнопка)
		перем ПериодПланирования;
	Форма = Документы.ПроверкаНагрузкиДисков.ПолучитьФорму("ФормаСвода");
	форма.Открыть();

КонецПроцедуры

Процедура ДокументСписокПриПолученииДанных(Элемент, ОформленияСтрок)
	Для каждого стр из ОформленияСтрок Цикл
		док = стр.ДанныеСтроки.Ссылка;
		дубль = 0;
		впроцессе = 0;
		для каждого ст из док.товары цикл
			дубль = дубль+1*ст.дубль;
			впроцессе = впроцессе+1*ст.впроцессе;
		конецЦИкла;	
		стр.Ячейки.Дубли.Текст = Дубль;
		стр.Ячейки.Дубли.ОтображатьТекст = истина;
		стр.Ячейки.НеЗаконченные.Текст =впроцессе;
		стр.Ячейки.НеЗаконченные.ОтображатьТекст = истина;

	конецЦикла;	
КонецПроцедуры
