
Процедура ДокументыПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)

	Если Элемент.Колонки.ВидСкидки.Видимость Тогда
		ОформлениеСтроки.Ячейки.ВидСкидки.ОтображатьТекст = Истина;
		ОформлениеСтроки.Ячейки.ВидСкидки.Текст = ДанныеСтроки.УстановкаСкидокНоменклатуры.ВидСкидки;
	КонецЕсли;
	Если Элемент.Колонки.ДатаНачала.Видимость Тогда
		ОформлениеСтроки.Ячейки.ДатаНачала.ОтображатьТекст = Истина;
		ОформлениеСтроки.Ячейки.ДатаНачала.Текст = Формат(ДанныеСтроки.УстановкаСкидокНоменклатуры.ДатаНачала, "ДЛФ = Д");
	КонецЕсли;
	Если Элемент.Колонки.ДатаОкончания.Видимость Тогда
		ОформлениеСтроки.Ячейки.ДатаОкончания.ОтображатьТекст = Истина;
		ОформлениеСтроки.Ячейки.ДатаОкончания.Текст = Формат(ДанныеСтроки.УстановкаСкидокНоменклатуры.ДатаОкончания, "ДЛФ = Д");
	КонецЕсли;
	Если Элемент.Колонки.Условие.Видимость Тогда
		ОформлениеСтроки.Ячейки.Условие.ОтображатьТекст = Истина;
		ОформлениеСтроки.Ячейки.Условие.Текст = ДанныеСтроки.УстановкаСкидокНоменклатуры.Условие;
	КонецЕсли;
	Если Элемент.Колонки.ТипСкидкиНаценки.Видимость Тогда
		ОформлениеСтроки.Ячейки.ТипСкидкиНаценки.ОтображатьТекст = Истина;
		ОформлениеСтроки.Ячейки.ТипСкидкиНаценки.Текст = ДанныеСтроки.УстановкаСкидокНоменклатуры.ТипСкидкиНаценки;
	КонецЕсли;

КонецПроцедуры
Процедура ДокументыУстановкаСкидокНоменклатурыПриИзменении(Элемент)
	Если Документы.НайтиСтроки(Новый Структура("УстановкаСкидокНоменклатуры", Элемент.Значение)).Количество()>1 Тогда
		Сообщить ("Документ: " + Элемент.Значение + " уже выбран в табличной части");
		Элемент.Значение = Неопределено;
	КонецЕсли;
КонецПроцедуры

// Процедура вызова структуры подчиненности документа
Процедура ДействияФормыСтруктураПодчиненностиДокумента(Кнопка)
	ПоказатьСтруктуруПодчиненностиДокумента(Ссылка);
КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	яштСменаПользователя(ЭтотОбъект, ЭтаФорма, глТекущийПользователь);
КонецПроцедуры

Процедура ПриОткрытии()
	Если НЕ ЭтоНовый() Тогда
		
		УстановитьЭлементыИсторииКнопка(Ссылка, ЭтаФорма);
		
	КонецЕсли; 

	
КонецПроцедуры

Процедура ВыводИстории(Элемент)
	
	ВывестиИсторию(Ссылка, ЭтаФорма);
	
КонецПроцедуры
	
	
