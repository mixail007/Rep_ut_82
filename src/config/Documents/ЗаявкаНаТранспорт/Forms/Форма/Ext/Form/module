
Процедура ОсновныеДействияФормыОК(Кнопка)
	Если ЗначениеНеЗаполнено(Сум) или ЗначениеНеЗаполнено(дат) Тогда
		сообщить("Обязательно заполните все поля!!!");
	иначе
		
	Рез = Новый Структура;
	Рез.Вставить("Сумма",Сум);
	Рез.Вставить("Дата",Дат);
	ЭтаФорма.Закрыть(Рез);
	
	КонецЕсли;
КонецПроцедуры
