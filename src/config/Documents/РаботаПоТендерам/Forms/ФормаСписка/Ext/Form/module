
Процедура ПриОткрытии()
	Если НЕ ( глТекущийПользователь.направлениеПродаж = справочники.НаправленияПродаж.Тендеры
		   или РольДоступна("ПравоЗавершенияРаботыПользователей") //IT-отдел
		   или глТекущийПользователь.НаправлениеПродаж.Руководитель = глТекущийПользователь // Марешева, Бондаренков, Филатова, Горелов
		   или глТекущийПользователь.ОсновноеПодразделение.Руководитль = глТекущийПользователь // Серков
		    ) тогда
		   Предупреждение("Данный документ предназначен для менеджеров по Тендерам!", 60);
		   ЭтаФорма.Закрыть();
	КонецЕсли;	   
КонецПроцедуры
