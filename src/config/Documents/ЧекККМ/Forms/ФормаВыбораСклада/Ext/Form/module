
Перем ВыборСкладаПроизведен;

Процедура ОсновныеДействияФормыОК(Кнопка)
	// Вставить содержимое обработчика.
	Если ЭлементыФормы.ТаблицаСклады.ТекущаяСтрока.Остаток>0 Тогда
	ВыборСкладаПроизведен=Истина;
	Закрыть(ЭлементыФормы.ТаблицаСклады.ТекущаяСтрока.Склад);
	КонецЕсли;
КонецПроцедуры

Процедура ПередЗакрытием(Отказ, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
		Отказ= не ВыборСкладаПроизведен;
КонецПроцедуры

Процедура ТаблицаСкладыВыбор(Элемент, ВыбраннаяСтрока, Колонка, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	Если ЭлементыФормы.ТаблицаСклады.ТекущаяСтрока.Остаток>0 Тогда
	ВыборСкладаПроизведен=Истина;	
	Закрыть(ЭлементыФормы.ТаблицаСклады.ТекущаяСтрока.Склад);
    КонецЕсли;
КонецПроцедуры

ВыборСкладаПроизведен=Ложь;