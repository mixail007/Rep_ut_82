
Процедура ОбработкаПроведения(Отказ, Режим)
	Движения.УчетДенежныхСредствВПодотчетеУВодителей.Записывать = Истина;
	Движения.УчетДенежныхСредствВПодотчетеУВодителей.Очистить();
	Для Каждого ТекСтрокаРасшифровка Из Расшифровка Цикл
		Движение = Движения.УчетДенежныхСредствВПодотчетеУВодителей.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
		Движение.Период = Дата;
		Движение.Заказ = ТекСтрокаРасшифровка.Заказ;
		Движение.Реализация = ТекСтрокаРасшифровка.Реализация;
		Движение.ЗаданиеНаОтгрузку = ЗаданиеНаОтгрузку;
		Движение.Сумма = ТекСтрокаРасшифровка.Сумма;
		Движение.СуммаТранспорт = ТекСтрокаРасшифровка.СуммаТранспорт;
		Движение.СуммаПредыдущийДолг = ТекСтрокаРасшифровка.СуммаПредыдущийДолг;
	КонецЦикла;

КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
		СуммаДокумента = Расшифровка.Итог("Сумма")+Расшифровка.Итог("СуммаТранспорт")+Расшифровка.Итог("СуммаПредыдущийДолг");
КонецПроцедуры
