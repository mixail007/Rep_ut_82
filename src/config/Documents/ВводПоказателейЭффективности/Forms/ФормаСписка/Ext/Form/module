
Процедура ПриОткрытии()
	
	Если ПолучитьЗначениеПоУмолчанию(ПараметрыСеанса.ТекущийПользователь, "УчетТолькоПоПодразделениюПользователя") Тогда
		Если Отбор.Найти("НаправлениеПродаж") = Неопределено Тогда 
			Отбор.Добавить("НаправлениеПродаж");
		КонецЕсли;
		Отбор["НаправлениеПродаж"].Использование = Истина;
		Отбор["НаправлениеПродаж"].ВидСравнения = ВидСравнения.Равно;
		Отбор["НаправлениеПродаж"].Значение = ПараметрыСеанса.ТекущийПользователь.НаправлениеПродаж;
		ЭлементыФормы.ДокументСписок.НастройкаОтбора["НаправлениеПродаж"].Доступность = Ложь;
	КонецЕсли;
	
КонецПроцедуры
