
Процедура КоманднаяПанель1Заполнить(Кнопка)
	ФормаЗаполнения = ПолучитьФорму("ФормаПараметровЗаполнения",Этаформа,Ссылка);
	ТабДляЗаполнения = ФормаЗаполнения.ОткрытьМодально();
	ОстаткиПоЗаказам.Загрузить(ТабДляЗаполнения);
КонецПроцедуры

Процедура ОстаткиПоЗаказамПриПолученииДанных(Элемент, ОформленияСтрок)
	Для каждого ОформлениеСтроки Из ОформленияСтрок Цикл
		Данныестроки = ОформлениеСтроки.Данныестроки;
		ОформлениеСтроки.Ячейки.Код.ОтображатьТекст = истина;
		ОформлениеСтроки.Ячейки.Код.Текст = Данныестроки.Номенклатура.Код;
	КонецЦикла;

КонецПроцедуры
