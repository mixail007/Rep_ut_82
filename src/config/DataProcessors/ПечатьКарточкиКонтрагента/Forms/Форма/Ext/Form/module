
Процедура КнопкаВыполнитьНажатие(Кнопка)
	
	Если ЗначениеНеЗаполнено(Контрагент) Тогда
		Сообщить("Необходимо выбрать контрагента!", СтатусСообщения.Важное);
		Возврат;
	КонецЕсли;
	
	Если Контрагент.ЭтоГруппа Тогда
		Сообщить("Карточку нельзя сформировать для группы элементов!", СтатусСообщения.Важное);
		Возврат;
	КонецЕСли;
	
	Печать();
	
КонецПроцедуры
