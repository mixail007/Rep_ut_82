
Процедура КнопкаВыполнитьНажатие(Кнопка)
	
	//общий модуль яштРезервыПоТоварам
	Если Вариант=0 тогда
		яштРезервыПоТоварам.ЗакрытиеРезервовНаСкладах(ДатаЗакрытия);
	иначе //Если Вариант=1 тогда
		яштРезервыПоТоварам.ЗакрытиеРезервовНаСкладах( , ЗаказЗакрытия);
	КонецЕсли;	
	
КонецПроцедуры
