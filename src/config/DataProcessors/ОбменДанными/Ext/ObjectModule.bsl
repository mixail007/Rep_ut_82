Процедура ВыполнитьОбмен() Экспорт
	
	Если ПланыОбмена.ОбменУТ_БП.ЭтотУзел() = УзелОбмена Тогда
		Сообщить("Нельзя выбирать этот узел"); 
		возврат
	КонецЕсли; 

	// Получаем объект узла обмена
	УзелОбъект = УзелОбмена.ПолучитьОбъект();
	// Читаем новое сообщение обмена
	УзелОбъект.ПрочитатьСообщениеСИзменениями();
	// Записываем новое сообщение обмена
	УзелОбъект.ЗаписатьСообщениеСИзменениями();
	
КонецПроцедуры
 