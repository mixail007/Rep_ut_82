
// Обработчик события "ПередЗаписью" Объекта
//
Процедура ПередЗаписью(Отказ)
	
	Если ЮрФизЛицо = Перечисления.ЮрФизЛицо.ФизЛицо Тогда
		КПП       = "";
		КодПоОКПО = "";
	КонецЕсли; 
	
	Если НЕ Отказ Тогда
	
		обЗаписатьПротоколИзменений(ЭтотОбъект);
	
	КонецЕсли; 
	
КонецПроцедуры

функция ПечатьКартыПатнера(орг) Экспорт
	
	макет = ПолучитьМакет("КартаПартнера");
	КомОбъект = макет.Получить();
	КомОбъект.Application.Visible = Истина;

//-----------------замена полей-----------------------------------------------------------------------	
	//спис = новый СписокЗначений;
	//спис.Добавить( орг.ИНН, "[ИНН]");
	//спис.Добавить( орг.КПП, "[КПП]");
	//
	//КомОбъект.Application.Activate();
	//Документ = КомОбъект.Application.ActiveDocument;
	//  для i=0 по спис.Количество()-1 цикл
	//	Замена = Документ.Content.Find;      
	//  	Замена.Execute(спис[i].Представление, Ложь, Истина, Ложь, , , Истина, , Ложь, спис[i].Значение);
	//КонецЦикла;
//---------------------------------------------------------------------------------------------------
//Файл = КаталогВременныхФайлов() + "Карта Клиента.doc";
//КомОбъект.Application.SaveAs(Файл);

КонецФункции	