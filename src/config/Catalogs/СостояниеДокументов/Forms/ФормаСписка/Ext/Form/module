
Процедура СправочникСписокЦветСостоянияНачалоВыбора(Элемент, СтандартнаяОбработка)
	
	Элемент.СписокВыбора = ПолучитьСписокЦветов();  //яштПрочее.
	
КонецПроцедуры

Процедура СправочникСписокПриПолученииДанных(Элемент, ОформленияСтрок)
	
	Для каждого стр1 из ОформленияСтрок цикл
		Если стр1.ДанныеСтроки.ЦветСостояния<>"" тогда
			стр1.ЦветФона = webЦвета[стр1.ДанныеСтроки.ЦветСостояния];
		КонецЕсли;	
	КонецЦикла;	
	
КонецПроцедуры
