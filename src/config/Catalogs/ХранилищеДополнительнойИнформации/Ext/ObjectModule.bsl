
Процедура ПередЗаписью(Отказ)
	
	Если ТипЗнч(Объект) = Тип("ДокументСсылка.ЭлектронноеПисьмо") Тогда
		Если НЕ ОбменДанными.Загрузка Тогда
			Попытка
				ЗначениеРазделенияДоступа = Объект.УчетнаяЗапись;
			Исключение
				СообщитьОбОшибке(ОписаниеОшибки());
				Отказ = Истина;
			КонецПопытки;
		КонецЕсли; 
	Иначе
		ЗначениеРазделенияДоступа = Справочники.УчетныеЗаписиЭлектроннойПочты.ПустаяСсылка();
	КонецЕсли; 
	
	//ОС и НМА
	
	Если Не ОбменДанными.Загрузка И ТипЗнч(Объект) = Тип("СправочникСсылка.Номенклатура") Тогда
		
	//	ПроверитьРегистрацию(Объект, ЭтотОбъект);	
		
	КонецЕсли; 

	//----ОС и НМА
	
	Если НЕ Отказ Тогда
	
		обЗаписатьПротоколИзменений(ЭтотОбъект);
	
	КонецЕсли; 

	
КонецПроцедуры

