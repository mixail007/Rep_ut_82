

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	ОтборПоПодразделению = СправочникСписок.Отбор.Найти("Подразделение");
	
	//+++ 12.01.2018 уже формируется отбор из 665 обработки!
	Если НЕ ЗначениеЗаполнено(ОтборПоПодразделению.Значение) тогда  
		ОтборПоПодразделению.ВидСравнения = ВидСравнения.Равно;
		ОтборПоПодразделению.Значение = глТекущийПользователь.ОсновноеПодразделение;
		ОтборПоПодразделению.Использование = Истина;
	КонецЕсли;

	ЭлементыФормы.СправочникСписок.НастройкаОтбора.Найти("Подразделение").Доступность = Ложь;
КонецПроцедуры
