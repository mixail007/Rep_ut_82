
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ОбменДанными.Загрузка Тогда
		Если НЕ ПолучитьЗначениеПоУмолчанию(ПараметрыСеанса.ТекущийПользователь, "ТипыДоговоровРазрешитьРедактировать") Тогда
			Сообщить("Вам запрещено добавлять/редактировать/помечать на удаление Типы договоров!");
			Отказ = Истина;
			Возврат;
		КонецЕсли;
	КонецЕсли;	
	
	Если НЕ Отказ Тогда
	
		обЗаписатьПротоколИзменений(ЭтотОбъект);
	
	КонецЕсли; 

КонецПроцедуры
