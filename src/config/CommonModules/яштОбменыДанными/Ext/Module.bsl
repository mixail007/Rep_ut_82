
//***2019.04.02
//выгрузка данных в бухгалтерию ФА+
Процедура ОбменУТ_ФАП() Экспорт
	
	ВыборкаУзлов = ПланыОбмена.ПланОбменаУТ_БП_ФАП.Выбрать();
	Пока ВыборкаУзлов.Следующий() Цикл
		Если ВыборкаУзлов.Ссылка = ПланыОбмена.ПланОбменаУТ_БП_ФАП.ЭтотУзел() Тогда
			Продолжить;
		Иначе
			УзелОбъект = ВыборкаУзлов.Ссылка.ПолучитьОбъект();
			УзелОбъект.ВыполнитьОбменУТФАП(ВыборкаУзлов.Ссылка);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры
