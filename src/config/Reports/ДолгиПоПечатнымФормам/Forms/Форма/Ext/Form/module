//перем Документ Экспорт
Процедура ОсновныеДействияФормыЗаписать(Кнопка)
	отбор = новый структура();
	Отбор.Вставить("Документ",Документ);
	Отбор.Вставить("ВидПечатнойформы",ВидПечатнойФормы);
	Запись  = РегистрыСведений.ПечатныеФормыВНаличии.Получить(Отбор);
	  ЗаписьМ = РегистрыСведений.ПечатныеФормыВНаличии.СоздатьМенеджерЗаписи();
	  ЗаполнитьЗначенияСвойств(ЗаписьМ,Запись);
	  ЗаписьМ.ВидПечатнойФормы = ВидПечатнойФормы;
	  Если ЗаписьМ.статус  = Справочники.СостояниеДокументов.ПустаяСсылка() тогда// записи нет
		ЗаписьМ.статус = Справочники.СостояниеДокументов.Требуется;
	  конецЕсли;	
	  ЗаписьМ.Документ = Документ;
	  ЗаписьМ.КомментарийМенеджера = комментарий;
	  ЗаписьМ.Записать(Истина);
	  Закрыть();
	
КонецПроцедуры
