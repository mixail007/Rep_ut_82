
Процедура ДействияФормыЗагрузить(Кнопка)
	Форм=РегистрыСведений.ВременныеФото.ПолучитьФорму("ФормыЗагрузкиИзЕкселя");
	
	СписокЗнач=Форм.ОткрытьМодально();
	Попытка
		Для Каждого стр из СписокЗнач цикл
			Фото = СокрЛП(стр.Значение);
			Рег = РегистрыСведений.ВременныеФото.СоздатьМенеджерЗаписи();
			Рег.ИмяФото = Фото;
			Рег.Записать();
    	КонецЦикла;
	Исключение
	КонецПопытки;

КонецПроцедуры
