
Процедура ПередЗаписью(Отказ, Замещение)
	
	//ОС и НМА
	
	Для каждого Запись Из ЭтотОбъект Цикл
		
		Если Не ОбменДанными.Загрузка И ТипЗнч(Запись.Объект) = Тип("СправочникСсылка.Номенклатура") Тогда
			
			ПроверитьРегистрацию(Запись.Объект, ЭтотОбъект);	
			
		КонецЕсли; 
		
	КонецЦикла; 
	
	//ОС и НМА

КонецПроцедуры
