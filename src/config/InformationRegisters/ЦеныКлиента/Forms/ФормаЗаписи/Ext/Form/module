

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	Если НЕ РольДоступна("ПолныеПрава") тогда
		СтандартнаяОбработка = ЛОЖЬ;
    	Отказ = Истина;
		Предупреждение("У Вас недостаточно прав для редактирования Цен клиентов!",30);
	КонецЕсли;
КонецПроцедуры

