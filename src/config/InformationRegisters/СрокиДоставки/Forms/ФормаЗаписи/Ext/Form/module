
Процедура ПриОткрытии()
	если не РольДоступна("ПравоЗавершенияРаботыПользователей") Тогда
		ЭлементыФормы.Код.Доступность = Ложь;
		ЭлементыФормы.Поставщик.Доступность = Ложь;
		ЭлементыФормы.СрокДоставки.Доступность = Ложь;
		
		ЭлементыФормы.ОТХ.Доступность = Ложь;
		ЭлементыФормы.ОтправлятьЗаказыПоставщику.Доступность = Ложь;
		ЭлементыФормы.ОдинЗаказПоставщикуВДень.Доступность = Ложь;
	КонецЕсли;
КонецПроцедуры

Процедура ДействияФормыОграничения(Кнопка)
AVS = справочники.Контрагенты.НайтиПоКоду("95695");
Берг= справочники.Контрагенты.НайтиПоКоду("П000727");
Если Поставщик=Берг или Поставщик=AVS тогда	
	форма1 = РегистрыСведений.ОтборДляЗагружаемойНоменклатурыПоАксам.ПолучитьФорму("ФормаСписка");
иначе
	форма1 = РегистрыСведений.ОтборДляЗагружаемойНоменлатурыПоставщиков.ПолучитьФорму("ФормаСписка");
	форма1.РегистрСведенийСписок.Отбор.Контрагент.Установить(Поставщик);
	форма1.ЭлементыФормы.РегистрСведенийСписок.НастройкаОтбора.Контрагент.Доступность = Ложь;
КонецЕсли;	
	форма1.Открыть();
	
КонецПроцедуры

Процедура ДействияФормыНаценкаАкс(Кнопка)
	
	форма1 = РегистрыСведений.НаценкаПоПроизводителямДляАксов.ПолучитьФорму("ФормаСписка");
	форма1.РегистрСведенийСписок.Отбор.Контрагент.Установить(Поставщик);
	форма1.ЭлементыФормы.РегистрСведенийСписок.НастройкаОтбора.Контрагент.Доступность = Ложь;
	форма1.Открыть();

КонецПроцедуры
