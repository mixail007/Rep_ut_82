
Процедура ДействияФормыПроверитьСтатусыЗаказовУПоставщикаПауэр(Кнопка)
	
	 Пауэр = Справочники.Контрагенты.НайтиПоРеквизиту("ИНН", "7703247653"); 
	СписЗаказов = новый СписокЗначений;
	
	Для каждого стр1 из ЭлементыФормы.РегистрСведенийСписок.Данные цикл
		Если стр1.Поставщик = Пауэр тогда
	      СписЗаказов.Добавить(стр1.ЗаказПокупателя, стр1.НомерДокумента);
		КонецЕсли;  
    КонецЦикла;
  
	яштПоставщики.ПауэрПроверкаСтатусов(СписЗаказов);
	
КонецПроцедуры
